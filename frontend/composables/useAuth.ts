// Copyright (c) 2025 The Linux Foundation and each contributor.
// SPDX-License-Identifier: MIT

/**
 * Auth composable for managing authentication state and methods
 * Use this composable in components to access Auth0 functionality
 * 
 * Note: In components, you can directly use the Auth0 composables:
 * - useAuth0() for accessing the Auth0 instance
 * - Or inject('$auth0') to get the Auth0 plugin instance
 */
export const useAuth = () => {
  return {
    // This is a placeholder composable
    // Use useAuth0() from @auth0/auth0-vue directly in components
  }
} 